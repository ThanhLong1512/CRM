"use client";

/**
 * QR scanner stub for product / shipment scanning (html5-qrcode).
 * Wire Html5QrcodeScanner in a useEffect when building the sales flow.
 */
export function QRScanner() {
  return (
    <div className="flex min-h-40 items-center justify-center rounded-lg border border-dashed border-border bg-muted/30 p-4 text-sm text-muted-foreground">
      QR Scanner placeholder
    </div>
  );
}
